import React, {FC} from 'react';
import {StyleSheet, ScrollView, View, Text} from 'react-native';

const TopRestaurants: FC<{}> = () => {
  return (
    <>
      <Text> top Restaurants</Text>
    </>
  );
};

const styles = StyleSheet.create({});

export default TopRestaurants;
